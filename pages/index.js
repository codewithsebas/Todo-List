import TaskForm from "@/components/TaskForm";
import TasksList from "@/components/TasksList";

const Home = () => {
  return (
    <main>
      <TaskForm />
      <TasksList />
    </main>
  );
};

export default Home;
